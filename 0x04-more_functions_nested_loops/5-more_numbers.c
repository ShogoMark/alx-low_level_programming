#include "main.h"

/**
 * more_numbers - prints numbers 0 - 14 10times
 * followed by new line
 *
 * Return: Always 0
 */

void more_numbers(void)
{
	int n, lt;

	for (n = 0; n < 10; n++)
	{
		if (lt = 0; lt < 15; lt++)
		{
			if (lt >= 10)
				_putchar((lt / 10) + 48);
			_putchar((lt % 10) + 48);
		}
		_putchar('\n');
	}
}
