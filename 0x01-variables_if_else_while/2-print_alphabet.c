#include <stdio.h>
#include <unistd.h>

/**
 * main - alphabets
 *
 * Return: Always 0 (Success)
 *
 */

int main(void)
{

	char alpha;

		for (alpha = 'a'; alpha <= 'z'; alpha++)

			{

				putchar(alpha);

			}

				putchar('\n');

			return (0);

}
